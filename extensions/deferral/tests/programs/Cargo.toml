[workspace]
[package]
name = "openvm-deferral-test-programs"
version = "0.0.0"
edition = "2021"

[dependencies]
openvm = { path = "../../../../crates/toolchain/openvm", default-features = false }
openvm-platform = { path = "../../../../crates/toolchain/platform", default-features = false }
openvm-custom-insn = { path = "../../../../crates/toolchain/custom_insn", default-features = false }
openvm-deferral-guest = { path = "../../guest", default-features = false }

[features]
default = []
std = ["openvm/std"]

[profile.release]
panic = "abort"
lto = "thin"
